import React, { ReactElement } from 'react';
import CounterContainer from '../containers/counter-container';

const SagaCounterPage = (): ReactElement => {
  return (
    <>
      <CounterContainer />
    </>
  );
};

export default SagaCounterPage;
