import React, { ReactElement } from 'react';
import { Layout } from 'components';

const MainPage = (): ReactElement => {
  return <Layout displayMain>여기는 메인 페이지</Layout>;
};

export default MainPage;
